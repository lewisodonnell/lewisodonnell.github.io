---
import { SITE, LINKS } from '../lib/site';
import { hasCV } from '../lib/assets';

const links = [
  { label: 'Email', url: `mailto:${SITE.email}` },
  ...LINKS.filter((l) => l.url),
  // The CV link appears automatically once public/cv.pdf exists.
  ...(hasCV ? [{ label: 'CV', url: '/cv.pdf' }] : []),
];
---

<div class="masthead">
  <h1>{SITE.name}</h1>
  <p class="tagline">{SITE.tagline}</p>
  <nav aria-label="Profile links">
    <ul class="links" role="list">
      {links.map((l) => <li><a href={l.url!}>{l.label}</a></li>)}
    </ul>
  </nav>
</div>

<style>
  /* Full-height hero panel; the inline script parallaxes it away on scroll. */
  .masthead {
    min-height: min(84vh, 50rem);
    display: flex;
    flex-direction: column;
    justify-content: center;
  }

  h1 {
    font-size: clamp(2.625rem, 7.5vw, 5rem);
    line-height: 1.08;
    letter-spacing: -0.01em;
  }

  @media (max-width: 860px) {
    .masthead {
      min-height: 58vh;
    }
  }

  .tagline {
    font-family: var(--font-display);
    font-optical-sizing: auto;
    font-size: clamp(var(--step-1), 3vw, 1.6rem);
    line-height: 1.35;
    margin: var(--space-3) 0 0;
  }

  .links {
    list-style: none;
    display: flex;
    flex-wrap: wrap;
    gap: var(--space-2) var(--space-4);
    margin: var(--space-4) 0 0;
    padding: 0;
    font-family: var(--font-mono);
    font-size: var(--step--1);
  }
</style>
